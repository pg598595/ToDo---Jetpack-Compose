<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.ToDo" parent="Theme.Material3.DayNight.NoActionBar" />

    <style name="Theme.ToDo.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/light_cream</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/dark_red</item>
        <item name="postSplashScreenTheme">@style/Theme.ToDo</item>
    </style>

</resources>